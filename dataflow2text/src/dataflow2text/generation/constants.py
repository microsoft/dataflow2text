# The default top-level act.
DEFAULT_ACT = "S"

# The act that should be used to render values as strings.
# The value is hard-coded in the dataflow2text.generation.template_factory._TEMPLATE_GRAMMAR.
TOSTRING_ACT = "toString"
